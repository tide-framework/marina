I am a page in Marina.

Each page has an author and a template. The #contents must be in the  Pillar syntax.

The template is used to build the htmlContents of the page, see #htmlContentsWithTemplate.
